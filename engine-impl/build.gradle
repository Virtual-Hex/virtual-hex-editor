group 'com.virtual-hex.editor'
version '1.0.0-SNAPSHOT'

sourceCompatibility = 1.8

dependencies {
    compile project(":engine-api")
    compile project(path: ':engine-api', configuration: 'compile')
}